/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findSessionPda } from "../pdas";
import { PINATA_PROGRAM_ADDRESS } from "../programs";

export const REINITIALIZE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  135, 255, 205, 161, 223, 162, 94, 137,
]);

export function getReinitializeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REINITIALIZE_DISCRIMINATOR
  );
}

export type ReinitializeInstruction<
  TProgram extends string = typeof PINATA_PROGRAM_ADDRESS,
  TAccountGm extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGm extends string
        ? ReadonlySignerAccount<TAccountGm> & AccountSignerMeta<TAccountGm>
        : TAccountGm,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      ...TRemainingAccounts,
    ]
  >;

export type ReinitializeInstructionData = {
  discriminator: ReadonlyUint8Array;
  sessionId: string;
};

export type ReinitializeInstructionDataArgs = { sessionId: string };

export function getReinitializeInstructionDataEncoder(): Encoder<ReinitializeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["sessionId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: REINITIALIZE_DISCRIMINATOR })
  );
}

export function getReinitializeInstructionDataDecoder(): Decoder<ReinitializeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["sessionId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getReinitializeInstructionDataCodec(): Codec<
  ReinitializeInstructionDataArgs,
  ReinitializeInstructionData
> {
  return combineCodec(
    getReinitializeInstructionDataEncoder(),
    getReinitializeInstructionDataDecoder()
  );
}

export type ReinitializeAsyncInput<
  TAccountGm extends InstructionSignerInput = InstructionSignerInput,
  TAccountSession extends InstructionAccountInput = InstructionAccountInput,
> = {
  gm: TAccountGm;
  session?: TAccountSession;
  sessionId: ReinitializeInstructionDataArgs["sessionId"];
};

export async function getReinitializeInstructionAsync<
  TAccountGm extends InstructionSignerInput,
  TAccountSession extends InstructionAccountInput,
  TProgramAddress extends Address = typeof PINATA_PROGRAM_ADDRESS,
>(
  input: ReinitializeAsyncInput<TAccountGm, TAccountSession>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ReinitializeInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountGm,
      InstructionAccountInputAddress<TAccountGm>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSession,
      InstructionAccountInputAddress<TAccountSession>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PINATA_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    gm: { value: input.gm ?? null, isSigner: true, isWritable: false },
    session: {
      value: input.session ?? null,
      isSigner: false,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.session.value) {
    accounts.session.value = await findSessionPda(
      {
        sessionId: getNonNullResolvedInstructionInput(
          "sessionId",
          args.sessionId
        ),
      },
      { programAddress }
    );
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("gm", accounts.gm),
      getAccountMeta("session", accounts.session),
    ],
    data: getReinitializeInstructionDataEncoder().encode(
      args as ReinitializeInstructionDataArgs
    ),
    programAddress,
  } as ReinitializeInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountGm,
      InstructionAccountInputAddress<TAccountGm>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSession,
      InstructionAccountInputAddress<TAccountSession>
    >
  >);
}

export type ReinitializeInput<
  TAccountGm extends InstructionSignerInput = InstructionSignerInput,
  TAccountSession extends InstructionAccountInput = InstructionAccountInput,
> = {
  gm: TAccountGm;
  session: TAccountSession;
  sessionId: ReinitializeInstructionDataArgs["sessionId"];
};

export function getReinitializeInstruction<
  TAccountGm extends InstructionSignerInput,
  TAccountSession extends InstructionAccountInput,
  TProgramAddress extends Address = typeof PINATA_PROGRAM_ADDRESS,
>(
  input: ReinitializeInput<TAccountGm, TAccountSession>,
  config?: { programAddress?: TProgramAddress }
): ReinitializeInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountGm,
    InstructionAccountInputAddress<TAccountGm>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSession,
    InstructionAccountInputAddress<TAccountSession>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PINATA_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    gm: { value: input.gm ?? null, isSigner: true, isWritable: false },
    session: {
      value: input.session ?? null,
      isSigner: false,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("gm", accounts.gm),
      getAccountMeta("session", accounts.session),
    ],
    data: getReinitializeInstructionDataEncoder().encode(
      args as ReinitializeInstructionDataArgs
    ),
    programAddress,
  } as ReinitializeInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountGm,
      InstructionAccountInputAddress<TAccountGm>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSession,
      InstructionAccountInputAddress<TAccountSession>
    >
  >);
}

export type ParsedReinitializeInstruction<
  TProgram extends string = typeof PINATA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    gm: TAccountMetas[0];
    session: TAccountMetas[1];
  };
  data: ReinitializeInstructionData;
};

export function parseReinitializeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedReinitializeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { gm: getNextAccount(), session: getNextAccount() },
    data: getReinitializeInstructionDataDecoder().decode(instruction.data),
  };
}
